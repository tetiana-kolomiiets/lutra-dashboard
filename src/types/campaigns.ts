export type TCampaignReport = {
  id: number;
  name: string;
  customerId: string;
  customerName: string;
  channel: string;
  impressions: number;
  clicks: number;
  ctr: number;
  spend: number;
};
